# Core Dependencies
pypdf>=4.0
python-docx>=1.0
pytesseract>=0.3
pdf2image>=1.16
Pillow>=10.0
langdetect>=1.0
python-slugify>=8.0
PyYAML>=6.0

# Dashboard
nicegui>=2.0

# HTTP-Client (Ollama, Telegram-Webhook, externe APIs, Supabase)
httpx>=0.27

# Watch Folder
watchdog>=4.0

# macOS Menuleisten-App (optional, nur fuer Tray)
rumps>=0.4; sys_platform == 'darwin'
pywebview>=5.0

# ── Lern-Engine (eigenes, selbstlernendes Klassifikationssystem) ─────────────
# Einmalig ~275 MB Modell-Download beim ersten Start; danach vollstaendig offline.
# Multilinguale Embeddings: Deutsch, Englisch, Albanisch und 50+ weitere Sprachen.
sentence-transformers>=3.0
scikit-learn>=1.4
numpy>=1.24

# Optional: Externe LLM-Fallback-Provider (auskommentieren zum Aktivieren)
# openai>=1.0
# anthropic>=0.30
